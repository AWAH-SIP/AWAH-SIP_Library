# You can create user.mak file in PJ root directory to specify
# additional flags to compiler and linker. For example:
export CFLAGS += -mcpu=cortex-a72 -mtune=cortex-a72 -mfpu=neon-fp-armv8
export LDFLAGS += 

